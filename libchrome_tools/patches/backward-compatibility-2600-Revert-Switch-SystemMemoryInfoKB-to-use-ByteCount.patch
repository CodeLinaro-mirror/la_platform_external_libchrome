From fff6027cda42be4b64f944e31003f3f35350bdf6 Mon Sep 17 00:00:00 2001
From: Jae Hoon Kim <kimjae@chromium.org>
Date: Thu, 7 Aug 2025 13:56:07 -0700
Subject: [PATCH] Revert "Switch SystemMemoryInfoKB to use ByteCount"

This reverts commit 5ce907330e81af5b87b16e10ccf85e7e909f2f94.
---
 base/process/process_metrics.cc          |  6 +-
 base/process/process_metrics.h           | 68 ++++++++++----------
 base/process/process_metrics_apple.mm    | 17 +++--
 base/process/process_metrics_linux.cc    | 26 ++++----
 base/process/process_metrics_unittest.cc | 79 ++++++++++++------------
 base/system/sys_info.h                   |  4 +-
 base/system/sys_info_linux.cc            | 14 ++---
 base/system/sys_info_unittest.cc         | 29 +++++----
 8 files changed, 115 insertions(+), 128 deletions(-)

diff --git a/base/process/process_metrics.cc b/base/process/process_metrics.cc
index 4956f651b8..f885e56399 100644
--- a/base/process/process_metrics.cc
+++ b/base/process/process_metrics.cc
@@ -39,11 +39,11 @@ int CalculateEventsPerSecond(uint64_t event_count,
 
 }  // namespace
 
-SystemMemoryInfo::SystemMemoryInfo() = default;
+SystemMemoryInfoKB::SystemMemoryInfoKB() = default;
 
-SystemMemoryInfo::SystemMemoryInfo(const SystemMemoryInfo&) = default;
+SystemMemoryInfoKB::SystemMemoryInfoKB(const SystemMemoryInfoKB&) = default;
 
-SystemMemoryInfo& SystemMemoryInfo::operator=(const SystemMemoryInfo&) =
+SystemMemoryInfoKB& SystemMemoryInfoKB::operator=(const SystemMemoryInfoKB&) =
     default;
 
 SystemMetrics::SystemMetrics() {
diff --git a/base/process/process_metrics.h b/base/process/process_metrics.h
index 254a8f4e49..4ade83b7ed 100644
--- a/base/process/process_metrics.h
+++ b/base/process/process_metrics.h
@@ -15,7 +15,6 @@
 #include <string_view>
 
 #include "base/base_export.h"
-#include "base/byte_count.h"
 #include "base/gtest_prod_util.h"
 #include "base/memory/raw_ptr.h"
 #include "base/process/process_handle.h"
@@ -99,7 +98,7 @@ struct ProcessMemoryInfo {
 #endif  // BUILDFLAG(IS_WIN)
 
   // On iOS,
-  //   TBD: https://crbug.com/41315025
+  //   TBD: https://crbug.com/714961
 };
 
 // Provides performance metrics for a specified process (CPU usage and IO
@@ -312,28 +311,25 @@ BASE_EXPORT void IncreaseFdLimitTo(unsigned int max_descriptors);
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_APPLE) || BUILDFLAG(IS_LINUX) ||      \
     BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_AIX) || \
     BUILDFLAG(IS_FUCHSIA)
-// Data about system-wide memory consumption. Available on Windows, Mac, Linux,
-// Android and Chrome OS.
-//
-// The values are kept in ByteCount but depending on the platform, the
-// granularity might be at the KB level or higher.
+// Data about system-wide memory consumption. Values are in KB. Available on
+// Windows, Mac, Linux, Android and Chrome OS.
 //
 // Total memory are available on all platforms that implement
 // GetSystemMemoryInfo(). Total/free swap memory are available on all platforms
 // except on Mac. Buffers/cached/active_anon/inactive_anon/active_file/
 // inactive_file/dirty/reclaimable/pswpin/pswpout/pgmajfault are available on
 // Linux/Android/Chrome OS. Shmem/slab are Chrome OS only.
-// Speculative/file_backed/purgeable are Mac and iOS only. Free is absent on
-// Windows (see "avail_phys" below).
-struct BASE_EXPORT SystemMemoryInfo {
-  SystemMemoryInfo();
-  SystemMemoryInfo(const SystemMemoryInfo& other);
-  SystemMemoryInfo& operator=(const SystemMemoryInfo& other);
+// Speculative/file_backed/purgeable are Mac and iOS only.
+// Free is absent on Windows (see "avail_phys" below).
+struct BASE_EXPORT SystemMemoryInfoKB {
+  SystemMemoryInfoKB();
+  SystemMemoryInfoKB(const SystemMemoryInfoKB& other);
+  SystemMemoryInfoKB& operator=(const SystemMemoryInfoKB& other);
 
-  ByteCount total;
+  int total = 0;
 
 #if !BUILDFLAG(IS_WIN)
-  ByteCount free;
+  int free = 0;
 #endif
 
 #if BUILDFLAG(IS_WIN)
@@ -342,7 +338,7 @@ struct BASE_EXPORT SystemMemoryInfo {
   // size of the standby, free, and zero lists." (MSDN).
   // Standby: not modified pages of physical ram (file-backed memory) that are
   // not actively being used.
-  ByteCount avail_phys;
+  int avail_phys = 0;
 #endif
 
 #if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID) || \
@@ -352,36 +348,36 @@ struct BASE_EXPORT SystemMemoryInfo {
   // NOTE: this is ONLY valid in kernels 3.14 and up.  Its value will always
   // be 0 in earlier kernel versions.
   // Note: it includes _all_ file-backed memory (active + inactive).
-  ByteCount available;
+  int available = 0;
 #endif
 
 #if !BUILDFLAG(IS_APPLE)
-  ByteCount swap_total;
-  ByteCount swap_free;
+  int swap_total = 0;
+  int swap_free = 0;
 #endif
 
 #if BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || \
     BUILDFLAG(IS_AIX) || BUILDFLAG(IS_FUCHSIA)
-  ByteCount buffers;
-  ByteCount cached;
-  ByteCount active_anon;
-  ByteCount inactive_anon;
-  ByteCount active_file;
-  ByteCount inactive_file;
-  ByteCount dirty;
-  ByteCount reclaimable;
+  int buffers = 0;
+  int cached = 0;
+  int active_anon = 0;
+  int inactive_anon = 0;
+  int active_file = 0;
+  int inactive_file = 0;
+  int dirty = 0;
+  int reclaimable = 0;
 #endif  // BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_LINUX) ||
         // BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_AIX) BUILDFLAG(IS_FUCHSIA)
 
 #if BUILDFLAG(IS_CHROMEOS)
-  ByteCount shmem;
-  ByteCount slab;
+  int shmem = 0;
+  int slab = 0;
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
 #if BUILDFLAG(IS_APPLE)
-  ByteCount speculative;
-  ByteCount file_backed;
-  ByteCount purgeable;
+  int speculative = 0;
+  int file_backed = 0;
+  int purgeable = 0;
 #endif  // BUILDFLAG(IS_APPLE)
 };
 
@@ -391,7 +387,7 @@ struct BASE_EXPORT SystemMemoryInfo {
 //
 // Fills in the provided |meminfo| structure. Returns true on success.
 // Exposed for memory debugging widget.
-BASE_EXPORT bool GetSystemMemoryInfo(SystemMemoryInfo* meminfo);
+BASE_EXPORT bool GetSystemMemoryInfo(SystemMemoryInfoKB* meminfo);
 
 #endif  // BUILDFLAG(IS_WIN) || BUILDFLAG(IS_APPLE) || BUILDFLAG(IS_LINUX) ||
         // BUILDFLAG(IS_CHROMEOS) BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_AIX) ||
@@ -417,13 +413,13 @@ BASE_EXPORT extern const char kProcSelfExe[];
 // returns true on success or false for a parsing error
 // Exposed for testing.
 BASE_EXPORT bool ParseProcMeminfo(std::string_view input,
-                                  SystemMemoryInfo* meminfo);
+                                  SystemMemoryInfoKB* meminfo);
 
 // Returns the memory committed by the system in KBytes, as from
 // GetSystemCommitCharge(), using data from `meminfo` instead of /proc/meminfo.
 // Exposed for testing.
 BASE_EXPORT size_t
-GetSystemCommitChargeFromMeminfo(const SystemMemoryInfo& meminfo);
+GetSystemCommitChargeFromMeminfo(const SystemMemoryInfoKB& meminfo);
 
 // Data from /proc/vmstat.
 struct BASE_EXPORT VmStatInfo {
@@ -575,7 +571,7 @@ class BASE_EXPORT SystemMetrics {
 
   size_t committed_memory_;
 #if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
-  SystemMemoryInfo memory_info_;
+  SystemMemoryInfoKB memory_info_;
   VmStatInfo vmstat_info_;
   SystemDiskInfo disk_info_;
 #endif
diff --git a/base/process/process_metrics_apple.mm b/base/process/process_metrics_apple.mm
index 9b3b52421d..9e9f36b9c5 100644
--- a/base/process/process_metrics_apple.mm
+++ b/base/process/process_metrics_apple.mm
@@ -16,7 +16,6 @@
 
 #include "base/apple/mach_logging.h"
 #include "base/apple/scoped_mach_port.h"
-#include "base/byte_count.h"
 #include "base/containers/heap_array.h"
 #include "base/logging.h"
 #include "base/mac/mac_util.h"
@@ -253,8 +252,8 @@ size_t GetSystemCommitCharge() {
   return (data.active_count * PAGE_SIZE) / 1024;
 }
 
-bool GetSystemMemoryInfo(SystemMemoryInfo* meminfo) {
-  meminfo->total = ByteCount::FromUnsigned(SysInfo::AmountOfPhysicalMemory());
+bool GetSystemMemoryInfo(SystemMemoryInfoKB* meminfo) {
+  meminfo->total = static_cast<int>(SysInfo::AmountOfPhysicalMemory() / 1024);
 
   base::apple::ScopedMachSendRight host(mach_host_self());
   vm_statistics64_data_t vm_info;
@@ -299,8 +298,8 @@ bool GetSystemMemoryInfo(SystemMemoryInfo* meminfo) {
 #endif  // !(defined(IS_IOS) && defined(ARCH_CPU_X86_FAMILY))
 
   if (vm_info.speculative_count <= vm_info.free_count) {
-    meminfo->free = ByteCount::FromUnsigned(
-        PAGE_SIZE * (vm_info.free_count - vm_info.speculative_count));
+    meminfo->free = saturated_cast<int>(
+        PAGE_SIZE / 1024 * (vm_info.free_count - vm_info.speculative_count));
   } else {
     // Inside the `host_statistics64` call above, `speculative_count` is
     // computed later than `free_count`, so these values are snapshots of two
@@ -313,15 +312,15 @@ bool GetSystemMemoryInfo(SystemMemoryInfo* meminfo) {
     // inexact, but even in the case where `speculative_count` is less than
     // `free_count`, the computed `meminfo->free` will only be an approximation
     // given that the two inputs come from different points in time.
-    meminfo->free = ByteCount();
+    meminfo->free = 0;
   }
 
   meminfo->speculative =
-      ByteCount::FromUnsigned(PAGE_SIZE * vm_info.speculative_count);
+      saturated_cast<int>(PAGE_SIZE / 1024 * vm_info.speculative_count);
   meminfo->file_backed =
-      ByteCount::FromUnsigned(PAGE_SIZE * vm_info.external_page_count);
+      saturated_cast<int>(PAGE_SIZE / 1024 * vm_info.external_page_count);
   meminfo->purgeable =
-      ByteCount::FromUnsigned(PAGE_SIZE * vm_info.purgeable_count);
+      saturated_cast<int>(PAGE_SIZE / 1024 * vm_info.purgeable_count);
 
   return true;
 }
diff --git a/base/process/process_metrics_linux.cc b/base/process/process_metrics_linux.cc
index 64eac2c83a..c1fe2c2e11 100644
--- a/base/process/process_metrics_linux.cc
+++ b/base/process/process_metrics_linux.cc
@@ -20,7 +20,6 @@
 #include <string_view>
 #include <utility>
 
-#include "base/byte_count.h"
 #include "base/compiler_specific.h"
 #include "base/containers/span.h"
 #include "base/cpu.h"
@@ -267,20 +266,20 @@ ProcessMetrics::ProcessMetrics(ProcessHandle process) : process_(process) {}
 #endif
 
 size_t GetSystemCommitCharge() {
-  SystemMemoryInfo meminfo;
+  SystemMemoryInfoKB meminfo;
   if (!GetSystemMemoryInfo(&meminfo)) {
     return 0;
   }
   return GetSystemCommitChargeFromMeminfo(meminfo);
 }
 
-size_t GetSystemCommitChargeFromMeminfo(const SystemMemoryInfo& meminfo) {
-  // TODO(http://b/315988925): This math is incorrect: `cached` can be very
+size_t GetSystemCommitChargeFromMeminfo(const SystemMemoryInfoKB& meminfo) {
+  // TODO(crbug.com/315988925): This math is incorrect: `cached` can be very
   // large so that `free` + `buffers` + `cached` > `total`. Replace this with a
   // more meaningful metric or remove it. In the meantime, convert underflows to
   // 0 instead of crashing.
-  return ClampedNumeric<size_t>(meminfo.total.InKiB()) - meminfo.free.InKiB() -
-         meminfo.buffers.InKiB() - meminfo.cached.InKiB();
+  return ClampedNumeric<size_t>(meminfo.total) - meminfo.free -
+         meminfo.buffers - meminfo.cached;
 }
 
 int ParseProcStatCPU(std::string_view input) {
@@ -381,7 +380,7 @@ const size_t kDiskWeightedIOTime = 13;
 }  // namespace
 
 bool ParseProcMeminfo(std::string_view meminfo_data,
-                      SystemMemoryInfo* meminfo) {
+                      SystemMemoryInfoKB* meminfo) {
   // The format of /proc/meminfo is:
   //
   // MemTotal:      8235324 kB
@@ -394,7 +393,7 @@ bool ParseProcMeminfo(std::string_view meminfo_data,
 
   // As a basic sanity check at the end, make sure the MemTotal value will be at
   // least non-zero. So start off with a zero total.
-  meminfo->total = ByteCount(0);
+  meminfo->total = 0;
 
   for (std::string_view line : SplitStringPiece(
            meminfo_data, "\n", KEEP_WHITESPACE, SPLIT_WANT_NONEMPTY)) {
@@ -408,7 +407,7 @@ bool ParseProcMeminfo(std::string_view meminfo_data,
       continue;
     }
 
-    ByteCount* target = nullptr;
+    int* target = nullptr;
     if (tokens[0] == "MemTotal:") {
       target = &meminfo->total;
     } else if (tokens[0] == "MemFree:") {
@@ -446,15 +445,12 @@ bool ParseProcMeminfo(std::string_view meminfo_data,
     }
 #endif
     if (target) {
-      int64_t value;
-      if (StringToInt64(tokens[1], &value)) {
-        *target = KiB(value);
-      }
+      StringToInt(tokens[1], target);
     }
   }
 
   // Make sure the MemTotal is valid.
-  return meminfo->total > ByteCount(0);
+  return meminfo->total > 0;
 }
 
 bool ParseProcVmstat(std::string_view vmstat_data, VmStatInfo* vmstat) {
@@ -514,7 +510,7 @@ bool ParseProcVmstat(std::string_view vmstat_data, VmStatInfo* vmstat) {
   return has_pswpin && has_pswpout && has_pgmajfault;
 }
 
-bool GetSystemMemoryInfo(SystemMemoryInfo* meminfo) {
+bool GetSystemMemoryInfo(SystemMemoryInfoKB* meminfo) {
   // Used memory is: total - free - buffers - caches
   // ReadFileToStringNonBlocking doesn't require ScopedAllowIO, and reading
   // /proc/meminfo is fast. See crbug.com/1160988 for details.
diff --git a/base/process/process_metrics_unittest.cc b/base/process/process_metrics_unittest.cc
index 8fd66e649d..a360b0c8e6 100644
--- a/base/process/process_metrics_unittest.cc
+++ b/base/process/process_metrics_unittest.cc
@@ -20,7 +20,6 @@
 #include <utility>
 #include <vector>
 
-#include "base/byte_count.h"
 #include "base/command_line.h"
 #include "base/files/file.h"
 #include "base/files/file_path.h"
@@ -341,7 +340,7 @@ TEST_F(SystemMetricsTest, IsValidDiskName) {
 }
 
 TEST_F(SystemMetricsTest, ParseMeminfo) {
-  SystemMemoryInfo meminfo;
+  SystemMemoryInfoKB meminfo;
   const char invalid_input1[] = "abc";
   const char invalid_input2[] = "MemTotal:";
   // Partial file with no MemTotal
@@ -425,39 +424,39 @@ TEST_F(SystemMetricsTest, ParseMeminfo) {
       "Hugepagesize:     4096 kB\n";
 
   EXPECT_TRUE(ParseProcMeminfo(valid_input1, &meminfo));
-  EXPECT_EQ(meminfo.total.InKiB(), 3981504);
-  EXPECT_EQ(meminfo.free.InKiB(), 140764);
-  EXPECT_EQ(meminfo.available.InKiB(), 535413);
-  EXPECT_EQ(meminfo.buffers.InKiB(), 116480);
-  EXPECT_EQ(meminfo.cached.InKiB(), 406160);
-  EXPECT_EQ(meminfo.active_anon.InKiB(), 2972352);
-  EXPECT_EQ(meminfo.active_file.InKiB(), 179688);
-  EXPECT_EQ(meminfo.inactive_anon.InKiB(), 270108);
-  EXPECT_EQ(meminfo.inactive_file.InKiB(), 202748);
-  EXPECT_EQ(meminfo.swap_total.InKiB(), 5832280);
-  EXPECT_EQ(meminfo.swap_free.InKiB(), 3672368);
-  EXPECT_EQ(meminfo.dirty.InKiB(), 184);
-  EXPECT_EQ(meminfo.reclaimable.InKiB(), 30936);
+  EXPECT_EQ(meminfo.total, 3981504);
+  EXPECT_EQ(meminfo.free, 140764);
+  EXPECT_EQ(meminfo.available, 535413);
+  EXPECT_EQ(meminfo.buffers, 116480);
+  EXPECT_EQ(meminfo.cached, 406160);
+  EXPECT_EQ(meminfo.active_anon, 2972352);
+  EXPECT_EQ(meminfo.active_file, 179688);
+  EXPECT_EQ(meminfo.inactive_anon, 270108);
+  EXPECT_EQ(meminfo.inactive_file, 202748);
+  EXPECT_EQ(meminfo.swap_total, 5832280);
+  EXPECT_EQ(meminfo.swap_free, 3672368);
+  EXPECT_EQ(meminfo.dirty, 184);
+  EXPECT_EQ(meminfo.reclaimable, 30936);
 #if BUILDFLAG(IS_CHROMEOS)
-  EXPECT_EQ(meminfo.shmem.InKiB(), 140204);
-  EXPECT_EQ(meminfo.slab.InKiB(), 54212);
+  EXPECT_EQ(meminfo.shmem, 140204);
+  EXPECT_EQ(meminfo.slab, 54212);
 #endif
   EXPECT_EQ(355725u,
             base::SysInfo::AmountOfAvailablePhysicalMemory(meminfo) / 1024);
   // Simulate as if there is no MemAvailable.
-  meminfo.available = ByteCount(0);
+  meminfo.available = 0;
   EXPECT_EQ(374448u,
             base::SysInfo::AmountOfAvailablePhysicalMemory(meminfo) / 1024);
   meminfo = {};
   EXPECT_TRUE(ParseProcMeminfo(valid_input2, &meminfo));
-  EXPECT_EQ(meminfo.total.InKiB(), 255908);
-  EXPECT_EQ(meminfo.free.InKiB(), 69936);
-  EXPECT_EQ(meminfo.available.InKiB(), 0);
-  EXPECT_EQ(meminfo.buffers.InKiB(), 15812);
-  EXPECT_EQ(meminfo.cached.InKiB(), 115124);
-  EXPECT_EQ(meminfo.swap_total.InKiB(), 524280);
-  EXPECT_EQ(meminfo.swap_free.InKiB(), 524200);
-  EXPECT_EQ(meminfo.dirty.InKiB(), 4);
+  EXPECT_EQ(meminfo.total, 255908);
+  EXPECT_EQ(meminfo.free, 69936);
+  EXPECT_EQ(meminfo.available, 0);
+  EXPECT_EQ(meminfo.buffers, 15812);
+  EXPECT_EQ(meminfo.cached, 115124);
+  EXPECT_EQ(meminfo.swap_total, 524280);
+  EXPECT_EQ(meminfo.swap_free, 524200);
+  EXPECT_EQ(meminfo.dirty, 4);
   EXPECT_EQ(69936u,
             base::SysInfo::AmountOfAvailablePhysicalMemory(meminfo) / 1024);
 
@@ -509,10 +508,10 @@ TEST_F(SystemMetricsTest, ParseMeminfo) {
 
   meminfo = {};
   EXPECT_TRUE(ParseProcMeminfo(large_cache_input, &meminfo));
-  EXPECT_EQ(meminfo.total.InKiB(), 18025572);
-  EXPECT_EQ(meminfo.free.InKiB(), 13150176);
-  EXPECT_EQ(meminfo.buffers.InKiB(), 1524852);
-  EXPECT_EQ(meminfo.cached.InKiB(), 12645260);
+  EXPECT_EQ(meminfo.total, 18025572);
+  EXPECT_EQ(meminfo.free, 13150176);
+  EXPECT_EQ(meminfo.buffers, 1524852);
+  EXPECT_EQ(meminfo.cached, 12645260);
   EXPECT_EQ(GetSystemCommitChargeFromMeminfo(meminfo), 0u);
 }
 
@@ -802,21 +801,21 @@ TEST_F(SystemMetricsTest, ParseZramStat) {
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_APPLE) || BUILDFLAG(IS_LINUX) || \
     BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
 TEST(SystemMetrics2Test, GetSystemMemoryInfo) {
-  SystemMemoryInfo info;
+  SystemMemoryInfoKB info;
   EXPECT_TRUE(GetSystemMemoryInfo(&info));
 
   // Ensure each field received a value.
-  EXPECT_GT(info.total, ByteCount(0));
+  EXPECT_GT(info.total, 0);
 #if BUILDFLAG(IS_WIN)
-  EXPECT_GT(info.avail_phys, ByteCount(0));
+  EXPECT_GT(info.avail_phys, 0);
 #else
-  EXPECT_GT(info.free, ByteCount(0));
+  EXPECT_GT(info.free, 0);
 #endif
 #if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
-  EXPECT_GT(info.buffers, ByteCount(0));
-  EXPECT_GT(info.cached, ByteCount(0));
-  EXPECT_GT(info.active_anon + info.inactive_anon, ByteCount(0));
-  EXPECT_GT(info.active_file + info.inactive_file, ByteCount(0));
+  EXPECT_GT(info.buffers, 0);
+  EXPECT_GT(info.cached, 0);
+  EXPECT_GT(info.active_anon + info.inactive_anon, 0);
+  EXPECT_GT(info.active_file + info.inactive_file, 0);
 #endif  // BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) ||
         // BUILDFLAG(IS_ANDROID)
 
@@ -836,12 +835,12 @@ TEST(SystemMetrics2Test, GetSystemMemoryInfo) {
         // BUILDFLAG(IS_ANDROID)
 
 #if BUILDFLAG(IS_APPLE)
-  EXPECT_GT(info.file_backed, ByteCount(0));
+  EXPECT_GT(info.file_backed, 0);
 #endif
 
 #if BUILDFLAG(IS_CHROMEOS)
   // Chrome OS exposes shmem.
-  EXPECT_GT(info.shmem, ByteCount(0));
+  EXPECT_GT(info.shmem, 0);
   EXPECT_LT(info.shmem, info.total);
 #endif
 }
diff --git a/base/system/sys_info.h b/base/system/sys_info.h
index 0515616bcb..3470b510d3 100644
--- a/base/system/sys_info.h
+++ b/base/system/sys_info.h
@@ -50,7 +50,7 @@ class ScopedAmountOfPhysicalMemoryOverride;
 }
 
 class FilePath;
-struct SystemMemoryInfo;
+struct SystemMemoryInfoKB;
 
 class BASE_EXPORT SysInfo {
  public:
@@ -374,7 +374,7 @@ class BASE_EXPORT SysInfo {
 #if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID) || \
     BUILDFLAG(IS_AIX)
   static uint64_t AmountOfAvailablePhysicalMemory(
-      const SystemMemoryInfo& meminfo);
+      const SystemMemoryInfoKB& meminfo);
 #endif
 
   // Sets the amount of physical memory in MB for testing, thus allowing tests
diff --git a/base/system/sys_info_linux.cc b/base/system/sys_info_linux.cc
index b4e226af5e..a4be947243 100644
--- a/base/system/sys_info_linux.cc
+++ b/base/system/sys_info_linux.cc
@@ -17,7 +17,6 @@
 #include <sstream>
 #include <type_traits>
 
-#include "base/byte_count.h"
 #include "base/check.h"
 #include "base/files/file_util.h"
 #include "base/notreached.h"
@@ -59,7 +58,7 @@ uint64_t SysInfo::AmountOfPhysicalMemoryImpl() {
 
 // static
 uint64_t SysInfo::AmountOfAvailablePhysicalMemoryImpl() {
-  SystemMemoryInfo info;
+  SystemMemoryInfoKB info;
   if (!GetSystemMemoryInfo(&info)) {
     return 0;
   }
@@ -68,16 +67,15 @@ uint64_t SysInfo::AmountOfAvailablePhysicalMemoryImpl() {
 
 // static
 uint64_t SysInfo::AmountOfAvailablePhysicalMemory(
-    const SystemMemoryInfo& info) {
+    const SystemMemoryInfoKB& info) {
   // See details here:
   // https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git/commit/?id=34e431b0ae398fc54ea69ff85ec700722c9da773
   // The fallback logic (when there is no MemAvailable) would be more precise
   // if we had info about zones watermarks (/proc/zoneinfo).
-  ByteCount res =
-      !info.available.is_zero()
-          ? std::max(info.available - info.active_file, ByteCount(0))
-          : info.free + info.reclaimable + info.inactive_file;
-  return res.InBytesUnsigned();
+  int res_kb = info.available != 0
+                   ? std::max(info.available - info.active_file, 0)
+                   : info.free + info.reclaimable + info.inactive_file;
+  return checked_cast<uint64_t>(res_kb) * 1024;
 }
 
 // static
diff --git a/base/system/sys_info_unittest.cc b/base/system/sys_info_unittest.cc
index a897ce0f4f..614472aaf9 100644
--- a/base/system/sys_info_unittest.cc
+++ b/base/system/sys_info_unittest.cc
@@ -53,7 +53,7 @@ namespace base {
 // Some Android (Cast) test devices have a large portion of physical memory
 // reserved. During investigation, around 115-150 MB were seen reserved, so we
 // track this here with a factory of safety of 2.
-static constexpr ByteCount kReservedPhysicalMemory = MiB(300);
+static constexpr int kReservedPhysicalMemory = 300 * 1024;  // In _K_bytes.
 #endif  // BUILDFLAG(IS_ANDROID)
 
 using SysInfoTest = PlatformTest;
@@ -103,35 +103,34 @@ TEST_F(SysInfoTest, AmountOfMem) {
 #define MAYBE_AmountOfAvailablePhysicalMemory AmountOfAvailablePhysicalMemory
 #endif  // BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS)
 TEST_F(SysInfoTest, MAYBE_AmountOfAvailablePhysicalMemory) {
-  SystemMemoryInfo info;
+  // Note: info is in _K_bytes.
+  SystemMemoryInfoKB info;
   ASSERT_TRUE(GetSystemMemoryInfo(&info));
-  EXPECT_GT(info.free, ByteCount(0));
-  if (!info.available.is_zero()) {
+  EXPECT_GT(info.free, 0);
+  if (info.available != 0) {
     // If there is MemAvailable from kernel.
     EXPECT_LT(info.available, info.total);
-    const ByteCount amount =
-        ByteCount(SysInfo::AmountOfAvailablePhysicalMemory(info));
+    const uint64_t amount = SysInfo::AmountOfAvailablePhysicalMemory(info);
     // We aren't actually testing that it's correct, just that it's sane.
     // Available memory is |free - reserved + reclaimable (inactive, non-free)|.
     // On some android platforms, reserved is a substantial portion.
-    const ByteCount available =
+    const int available =
 #if BUILDFLAG(IS_ANDROID)
-        std::max(info.free - kReservedPhysicalMemory, ByteCount(0));
+        std::max(info.free - kReservedPhysicalMemory, 0);
 #else
         info.free;
 #endif  // BUILDFLAG(IS_ANDROID)
-    EXPECT_GT(amount, available);
-    EXPECT_LT(amount, info.available);
+    EXPECT_GT(amount, checked_cast<uint64_t>(available) * 1024);
+    EXPECT_LT(amount / 1024, checked_cast<uint64_t>(info.available));
     // Simulate as if there is no MemAvailable.
-    info.available = ByteCount(0);
+    info.available = 0;
   }
 
   // There is no MemAvailable. Check the fallback logic.
-  const ByteCount amount =
-      ByteCount(SysInfo::AmountOfAvailablePhysicalMemory(info));
+  const uint64_t amount = SysInfo::AmountOfAvailablePhysicalMemory(info);
   // We aren't actually testing that it's correct, just that it's sane.
-  EXPECT_GT(amount, info.free);
-  EXPECT_LT(amount, info.total);
+  EXPECT_GT(amount, checked_cast<uint64_t>(info.free) * 1024);
+  EXPECT_LT(amount / 1024, checked_cast<uint64_t>(info.total));
 }
 #endif  // BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) ||
         // BUILDFLAG(IS_ANDROID)
-- 
2.51.0.rc0.205.g4a044479a3-goog

